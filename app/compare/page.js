import {shows} from '../../data/tour';export default function Compare(){const [a,b]=shows;return <div className="shell section"><p className="eyebrow">Show comparison</p><h1 className="pageTitle">Show #1 vs. Show #2</h1><div className="statsGrid"><article className="statCard"><span>Songs added</span><strong>0</strong></article><article className="statCard"><span>Songs dropped</span><strong>0</strong></article><article className="statCard"><span>Order changes</span><strong>0</strong></article><article className="statCard"><span>Similarity</span><strong>100%</strong></article></div><div className="detailGrid"><section className="panel"><h2>#{a.number} · {a.city}</h2>{a.setlist.map((s,i)=><p className="compareSong" key={s}><span>{i+1}</span>{s}</p>)}</section><section className="panel"><h2>#{b.number} · {b.city}</h2>{b.setlist.map((s,i)=><p className="compareSong" key={s}><span>{i+1}</span>{s}</p>)}</section></div></div>}