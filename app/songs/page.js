import Link from 'next/link';import {songs} from '../../data/tour';
export default function Songs(){return <div className="shell section"><p className="eyebrow">Song database</p><h1 className="pageTitle">Tour rotation</h1><div className="songTable"><div className="songTableHead"><span>Song</span><span>Appearances</span><span>Streak</span><span>Role</span></div>{songs.map(s=><Link className="songRow" key={s.slug} href={`/songs/${s.slug}`}><strong>{s.title}{s.liveDebut&&<b className="debut">Live debut</b>}</strong><span>{s.appearances}</span><span>{s.streak}</span><span>{s.opener?'Opener':s.closer?'Closer':'Set'}</span></Link>)}</div></div>}