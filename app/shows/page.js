import Link from 'next/link';import {schedule,shows} from '../../data/tour';
export default function Shows(){return <div className="shell section"><p className="eyebrow">Complete itinerary</p><h1 className="pageTitle">North American Tour</h1><div className="schedule">{schedule.map(item=>{const found=shows.find(s=>s.number===item.number);return <article className={`scheduleRow ${item.status}`} key={item.number}><div className="dateBlock"><strong>{new Date(item.date+'T12:00:00').toLocaleDateString('en-US',{month:'short',day:'numeric'})}</strong><small>2026</small></div><div><span>Show #{item.number}</span><h2>{item.venue}</h2><p>{item.city}</p></div><div className="rowAction">{found?<Link className="button smallButton" href={`/shows/${found.slug}`}>View report</Link>:<span className="status">Upcoming</span>}</div></article>})}</div></div>}