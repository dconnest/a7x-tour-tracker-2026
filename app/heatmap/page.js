import {songs,shows} from '../../data/tour';export default function Heatmap(){return <div className="shell section"><p className="eyebrow">Setlist heat map</p><h1 className="pageTitle">Song usage by show</h1><div className="heatmap"><div></div>{shows.map(s=><strong key={s.slug}>#{s.number}</strong>)}{songs.map(song=><div className="heatRow" key={song.slug}><span>{song.title}</span>{shows.map(show=><i className={show.setlist.includes(song.title)?'played':''} key={show.slug}/>)}</div>)}</div></div>}